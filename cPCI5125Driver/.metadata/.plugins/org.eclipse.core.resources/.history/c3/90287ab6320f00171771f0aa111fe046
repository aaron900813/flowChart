ifneq ($(KERNELRELEASE),)
obj-m := cPCI5125Test  
else
PWD  := $(shell pwd)
KERNELDIR ?= /lib/modules/$(shell uname -r)/build
all:
	$(MAKE) -C $(KERNELDIR) M=$(PWD)
clean:
	rm -rf .*.cmd *.o *.mod.c *.ko .tmp_versions
endif